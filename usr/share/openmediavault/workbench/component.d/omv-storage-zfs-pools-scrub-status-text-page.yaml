version: "1.0"
type: component
data:
  name: omv-storage-zfs-pools-scrub-status-text-page
  type: textPage
  config:
    hasReloadButton: true
    request:
      service: Zfs
      get:
        method: getScrubStatus
        params:
          name: "{{ _routeParams.name }}"
