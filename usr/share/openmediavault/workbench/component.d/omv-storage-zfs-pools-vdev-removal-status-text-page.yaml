version: "1.0"
type: component
data:
  name: omv-storage-zfs-pools-vdev-removal-status-text-page
  type: textPage
  config:
    hasReloadButton: true
    request:
      service: Zfs
      get:
        method: getVdevRemovalStatus
        params:
          name: "{{ _routeParams.name }}"
