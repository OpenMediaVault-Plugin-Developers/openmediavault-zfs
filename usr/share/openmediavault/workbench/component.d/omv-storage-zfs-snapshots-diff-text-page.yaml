version: "1.0"
type: component
data:
  name: omv-storage-zfs-snapshots-diff-text-page
  type: textPage
  config:
    hasReloadButton: true
    request:
      service: Zfs
      get:
        method: getSnapshotDiff
        params:
          name: "{{ _routeParams.name }}"
